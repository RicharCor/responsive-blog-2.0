<%= form_for(@post) do |f| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this article from being saved:</h2>

      <ul>
      <% post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= f.text_field :title, placeholder: "Título" %>
  </div>

  <div>
    <%= f.text_area :body, placeholder: "Cuerpo" %>
  </div>

  <div>
    <% @tags.each do |tag| %>
      <%= check_box_tag "tags[]", tag.id %> <%= tag.title %>
    <% end %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= form_for(@twitch) do |form| %>
  <% if twitch.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(twitch.errors.count, "error") %> prohibited this twitch from being saved:</h2>

      <ul>
      <% twitch.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :username %>
    <%= form.text_field :username, placeholder: "Add your username" %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
